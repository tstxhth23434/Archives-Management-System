# DocumentManagementSystemApplication 项目结构说明

> 本文件只说明「每个目录/文件放什么」，不编写具体代码。

---

## 一、根目录

```
DocumentManagementSystem/
├── pom.xml                          # Maven 构建配置（依赖、插件、版本管理）
├── .gitignore                       # Git 忽略规则
├── README.md                        # 项目说明（技术栈、启动步骤、功能截图）
├── doc/                             # 文档目录
│   └── database/                    # 数据库设计
│       ├── DocumentManagementSystemApplication.sql      # DDL 脚本
│       └── DocumentManagementSystemApplication_ER.svg   # E-R 图
├── src/                             # 后端源码（Spring Boot）
└── dms-ui/                          # 前端源码（Vue 3 + Vite）
```

---

## 二、后端目录结构（src/main/java）

包根路径：`com.example.documentmanagementsystem`

### 2.1 common — 公共/基础设施层

| 目录 | 用途 |
|---|---|
| `common/annotation` | 自定义注解。如 `@OpLog`（标记操作日志）、`@DataScope`（标记数据权限） |
| `common/aspect` | AOP 切面。如 `OpLogAspect`（拦截 `@OpLog` 写 sys_log 表） |
| `common/base` | 公共基类。如 `BaseEntity`（公共字段）、`BaseController`（统一返回方法）、`PageQuery`（分页参数基类） |
| `common/config` | 配置类。如 `MybatisPlusConfig`（分页插件+字段填充）、`SaTokenConfig`（登录鉴权拦截器）、`Knife4jConfig`（接口文档）、`WebMvcConfig`（跨域+静态资源映射） |
| `common/constant` | 常量枚举。如 `CommonConstant`（状态码、删除标志常量） |
| `common/exception` | 异常体系。如 `ServiceException`（业务异常）、`GlobalExceptionHandler`（全局异常统一返回） |
| `common/result` | 统一返回体。如 `Result<T>`（code/msg/data 标准格式） |
| `common/utils` | 工具类。如 `FileUploadUtil`（文件上传/路径生成）、`SecurityUtils`（获取当前登录用户） |

### 2.2 modules — 业务模块层

按功能模块分包，每个模块内部遵循 `controller → service → mapper → entity` 分层。

#### modules/system — 系统管理模块

| 目录 | 用途 |
|---|---|
| `system/controller` | 系统管理接口。如 `AuthController`（登录/登出）、`UserController`、`RoleController`、`MenuController`、`DictController`、`LogController` |
| `system/entity` | 实体类。如 `SysUser`、`SysRole`、`SysMenu`、`SysRoleMenu`、`SysDict`、`SysDictItem`、`SysLog` |
| `system/mapper` | MyBatis-Plus Mapper 接口。如 `SysUserMapper`、`SysRoleMapper` 等，继承 `BaseMapper<T>` |
| `system/service` | Service 接口。如 `ISysUserService`，定义业务方法 |
| `system/service/impl` | Service 实现。如 `SysUserServiceImpl`，继承 `ServiceImpl<Mapper, Entity>` |

#### modules/archive — 档案业务模块

| 目录 | 用途 |
|---|---|
| `archive/controller` | 档案业务接口。如 `FondsController`、`ArchiveTypeController`、`VolumeController`、`FileController`、`BorrowController`、`AppraisalController`、`DestructionController` |
| `archive/entity` | 实体类。如 `DaFonds`、`DaArchiveType`、`DaVolume`、`DaFile`、`DaElectronicFile`、`DaLifecycle`、`DaBorrow`、`DaAppraisal`、`DaDestruction`、`DaWarehouse`、`DaShelf`、`DaCell` |
| `archive/mapper` | Mapper 接口。如 `DaFileMapper`、`DaBorrowMapper` 等 |
| `archive/service` | Service 接口。如 `IDaFileService`、`IDaBorrowService` |
| `archive/service/impl` | Service 实现。如 `DaFileServiceImpl`（含档号生成逻辑） |

### 2.3 启动类

| 文件 | 用途 |
|---|---|
| `DocumentManagementSystemApplication.java` | Spring Boot 启动类，加 `@MapperScan` 扫描所有 Mapper |

---

## 三、后端资源目录（src/main/resources）

| 目录/文件 | 用途 |
|---|---|
| `application.yml` | 主配置文件（激活 dev、MyBatis-Plus、Sa-Token、Knife4j、上传路径等公共配置） |
| `application-dev.yml` | 开发环境配置（数据库连接、日志级别） |
| `application-prod.yml` | 生产环境配置（后续补充） |
| `mapper/` | 自定义 XML 映射文件（复杂 SQL 时手写，简单 CRUD 用 MyBatis-Plus 注解即可） |
| `static/` | 静态资源（Spring Boot 默认托管） |
| `templates/` | 模板文件（本项目前后端分离，此目录通常为空） |

---

## 四、前端目录结构（dms-ui）

```
dms-ui/
├── package.json              # 依赖与脚本（Vue3 + Vite + Element Plus + Pinia + Vue Router + Axios + ECharts）
├── vite.config.js            # Vite 配置（端口、代理、路径别名 @ → src）
├── index.html                # 入口 HTML
└── src/
    ├── main.js               # 入口：创建 Vue 实例、注册 Pinia/Router/ElementPlus/图标
    ├── App.vue               # 根组件（只放 <router-view />）
    ├── api/                  # 接口请求模块
    │   ├── request.js        # Axios 封装（基地址、拦截器、统一错误处理）
    │   ├── auth.js           # 登录相关接口（login/logout/getInfo）
    │   ├── user.js           # 用户管理接口
    │   ├── role.js           # 角色管理接口
    │   ├── menu.js           # 菜单管理接口
    │   ├── dict.js           # 字典管理接口
    │   ├── fonds.js          # 全宗管理接口
    │   ├── archiveType.js    # 门类管理接口
    │   ├── volume.js         # 案卷管理接口
    │   ├── file.js           # 档案文件接口
    │   ├── borrow.js         # 借阅管理接口
    │   └── dashboard.js      # 首页统计接口
    ├── assets/               # 静态资源（logo、背景图等）
    ├── components/           # 公共组件
    │   ├── Pagination.vue    # 分页组件（封装 el-pagination）
    │   ├── UploadFile.vue    # 文件上传组件
    │   ├── TreeFilter.vue    # 树形筛选组件
    │   └── AppTable.vue      # 通用表格组件
    ├── layouts/              # 布局组件
    │   └── MainLayout.vue    # 主布局：左侧菜单 + 顶栏 + 标签页 + 内容区
    ├── router/               # 路由配置
    │   └── index.js          # 路由表、路由守卫（登录校验、动态菜单）
    ├── stores/               # Pinia 状态管理
    │   ├── index.js          # Store 入口
    │   ├── user.js           # 用户状态（token、用户信息、权限码）
    │   ├── menu.js           # 菜单状态（菜单树、路由动态添加）
    │   └── tagsView.js       # 标签页状态（已打开页面、缓存）
    ├── styles/               # 全局样式
    │   ├── variables.scss    # SCSS 变量（主题色、侧边栏宽度）
    │   └── index.scss        # 全局样式重置、Element Plus 覆盖
    ├── utils/                # 工具函数
    │   ├── auth.js           # Token 读写（localStorage）
    │   ├── permission.js     # 权限校验函数
    │   └── date.js           # 日期格式化
    └── views/                # 页面视图
        ├── login/
        │   └── index.vue     # 登录页
        ├── dashboard/
        │   └── index.vue     # 首页驾驶舱（统计卡片 + ECharts 图表）
        ├── system/
        │   ├── user.vue      # 用户管理
        │   ├── role.vue      # 角色管理
        │   ├── menu.vue      # 菜单管理
        │   └── dict.vue      # 字典管理
        ├── archive/
        │   ├── fonds.vue     # 全宗管理
        │   ├── type.vue      # 门类管理
        │   ├── volume.vue    # 案卷管理
        │   ├── file.vue      # 档案文件列表/著录
        │   ├── fileDetail.vue# 档案详情（元数据 + 原文 + 生命周期时间轴）
        │   ├── borrow.vue    # 借阅管理（申请/审批/归还）
        │   ├── appraisal.vue # 鉴定管理
        │   └── destruction.vue# 销毁管理
        └── error/
            └── 404.vue       # 404 页面
```

---

## 五、关键设计约定

### 5.1 后端分层约定

```
Controller（薄） → Service（业务逻辑） → Mapper（数据访问） → Entity（表映射）
```

- **Controller**：只接收参数、调用 Service、返回 Result，不写业务逻辑
- **Service**：处理业务逻辑、事务控制（`@Transactional`）、调用 Mapper
- **Mapper**：继承 `BaseMapper<T>`，零 XML 写简单 CRUD；复杂 SQL 才写 XML
- **Entity**：继承 `BaseEntity`，字段与数据库表一一对应，用 Lombok `@Data`

### 5.2 公共字段约定

所有业务表实体继承 `BaseEntity`，自动拥有：

```
id, create_by, create_time, update_by, update_time, del_flag
```

`create_time/update_time` 由 MyBatis-Plus `MetaObjectHandler` 自动填充。
`create_by/update_by` 在 Controller 层从登录用户获取后手动设置。
`del_flag` 由 `@TableLogic` 自动处理逻辑删除。

### 5.3 接口路径约定

```
/api/auth/**      # 登录认证（开放，不拦截）
/api/system/**    # 系统管理
/api/archive/**   # 档案业务
/api/borrow/**    # 借阅业务
```

### 5.4 前端页面与接口对应

| 页面 | 调用 API 文件 | 后端 Controller |
|---|---|---|
| 登录页 | `api/auth.js` | `AuthController` |
| 用户管理 | `api/user.js` | `UserController` |
| 档案列表 | `api/file.js` | `FileController` |
| 借阅申请 | `api/borrow.js` | `BorrowController` |

---

## 六、每日开发对应目录速查

| 开发日 | 任务 | 主要涉及的目录 |
|---|---|---|
| D1 | 需求确认、初始化工程 | `pom.xml`、`dms-ui/package.json` |
| D2 | MyBatis-Plus、统一返回、全局异常 | `common/config`、`common/result`、`common/exception` |
| D3 | 系统表、Sa-Token 登录 | `modules/system/entity`、`modules/system/mapper`、`modules/system/controller` |
| D4 | 用户/角色 CRUD | `modules/system/**` |
| D5 | 菜单/权限 | `modules/system/controller/MenuController`、`modules/system/entity/SysMenu` |
| D6 | 字典、操作日志 AOP | `modules/system/entity/SysDict`、`common/annotation`、`common/aspect` |
| D7 | 前端起步 | `dms-ui/src/main.js`、`dms-ui/src/layouts`、`dms-ui/src/router` |
| D8 | 全宗/门类管理 | `modules/archive/entity/DaFonds`、`modules/archive/entity/DaArchiveType` |
| D9-D14 | 档案核心（案卷、文件、上传、导入） | `modules/archive/**`、`common/utils/FileUploadUtil` |
| D15-D21 | 生命周期、借阅、鉴定、销毁 | `modules/archive/entity/DaLifecycle`、`modules/archive/entity/DaBorrow` 等 |
| D22-D30 | 驾驶舱、库房、部署 | `views/dashboard/`、`modules/archive/entity/DaWarehouse` 等 |
