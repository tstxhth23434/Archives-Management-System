# D11 交付说明 — 案卷-文件从属关系展示

> 日期：2026-08-18
> 状态：✅ 完成并联调验证通过

## 一、本次交付

D10 已完成文件著录(题名/责任者/日期/保管期限/密级/关键词等字段齐全)。D11 补齐**案卷-文件从属关系**的双向展示:

| 页面 | 改动 |
|---|---|
| 案卷管理 `/archive/volume` | 操作列加"**文件**"按钮 → **案卷详情弹窗**:el-descriptions 展示案卷信息(档号/题名/年度/状态/保管期限)+ 该案卷下文件列表(档号/题名/责任者/文件日期/页数/状态) |
| 文件管理 `/archive/file` | 表格加"**所属案卷**"列(volumeId → 案卷题名映射,loadVolumeMap 拉取全部案卷构建 map,失败不影响列表) |

- 从属数据源:`pageFiles({ volumeId })` 按案卷过滤
- 案卷详情弹窗权限:archive:file:query;按钮 hasPerm 控制
- 空态:案卷无文件时显示"该案卷下暂无文件"

## 二、验证结果

| 用例 | 结果 |
|---|---|
| 新增案卷 → 挂 2 个文件 → 案卷详情过滤 | ✅ total=2,档号 JSXY-WS-2025-0001/0002 |
| 文件页所属案卷列 | ✅ volumeId→题名 正常映射 |
| Vite 编译 | ✅ volume/file 两页 200 |

## 三、下一步（D12）

- D12：电子原文上传(限制类型/大小,按 年/月 分目录存本地磁盘),档案详情页展示原文列表
